# encoding: utf-8
# Reajuste salarial

salario = float(input('Qual é o seu sálario atual? R$'))
reajuste = salario * 1.10
print('Seu salário, que era de R${}, foi reajustado para R${} a partir de fevereiro de 2022.'.format(salario, reajuste))
