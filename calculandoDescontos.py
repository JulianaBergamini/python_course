# encoding: utf-8
# Calculando descontos

preco = float(input('Qual é o preço do produto? R$'))
desconto = preco * 0.95
print('O produto que custava R${}, na promoção com desconto de 5% custará R${}!'.format(preco, desconto))